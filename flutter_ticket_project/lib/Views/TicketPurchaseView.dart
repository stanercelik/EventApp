import 'package:flutter/material.dart';

class TicketPurchaseView extends StatelessWidget {
  TicketPurchaseView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("asdsadasd"),
      ),
    );
  }
}
